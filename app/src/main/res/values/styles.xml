<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->

        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/primary_accent</item>
        <item name="android:textColorPrimary">@color/primary_text</item>

        <!-- Some how the custom button theme makes the buttons unusable on 4.x since using AppCompat.. worked before -->
        <!--<item name="android:buttonStyle">@style/ButtonStyle</item>-->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>

    <style name="defaultActivityStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:paddingBottom">@dimen/activity_vertical_margin</item>
        <item name="android:paddingLeft">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingRight">@dimen/activity_horizontal_margin</item>
        <item name="android:paddingTop">@dimen/activity_vertical_margin</item>
    </style>

    <style name="open_request_item_list_alignment">
        <item name="android:layout_gravity">center_vertical</item>
        <item name="android:layout_height">fill_parent</item>
    </style>

    <style name="ButtonStyle" parent="@style/Widget.AppCompat.ActionButton">
        <item name="android:background">@drawable/btn_custom</item>
        <item name="android:textColor">@color/primary_text</item>
    </style>

</resources>
